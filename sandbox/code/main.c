#include <core/interface.h>
#include <stdio.h>

// csConfig_t config;

int main( int argc, char **argv )
{
    config.winStartupW = 200;
    config.winStartupH = 200;
    // CS_Init( &config );
    // CS_Run();

    return 0;
}